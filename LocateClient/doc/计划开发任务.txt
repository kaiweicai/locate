1.服务器中的消息加上时间戳.
客户端收到该消息可以用该时间戳和本地时间进行比较.
注意这里未考虑时区.大部分用户应该使用标准北京时区的时间.
此功能代码已实现.实际不能使用.因为主机和服务器的系统不相同.

2.当同一个RIC被请求多次时.
是否每次请求都要去请求一次snapshot?
此功能已实现.

3.用户退订功能是否要加上.现在只能是客户关掉客户端.则服务器自动判断是否退订.

4.将xml格式转换成json格式进行网络传输.xml格式的数据大小是JSON的2倍.