1.服务器中的消息加上时间戳.
客户端收到该消息可以用该时间戳和本地时间进行比较.
注意这里未考虑时区.大部分用户应该使用标准北京时区的时间.
此功能代码已实现.实际不能使用.因为主机和服务器的系统不相同.

2.当同一个RIC被请求多次时.
是否每次请求都要去请求一次snapshot?
此功能已实现.

3.用户退订功能是否要加上.现在只能是客户关掉客户端.则服务器自动判断是否退订.

4.将xml格式转换成json格式进行网络传输.xml格式的数据大小是JSON的2倍. 未确定

5增加web请求报价能力.已实现

6.增加web端超时断开连接功能.

7.找到有其他用户订阅了产品,但是RFA没有继续发送该产品的报价.导致其他用户再次订阅该产品时只发送了一次snapshort的请求,不能接收到实时报价.

8.web客户端画股票曲线图功能完善.

9.开发一个管控平台对系统进行监视是否正常运行.

10.2014年七月七号莫名收到一个complete的讯号.RFA停止发送报价,随即socket关闭.是否和websorcket有关系,?原因待查.
RFA系统何时在何种状态下会发送一个complete的eventMessage.希望可以查RFA的日志给出原因.收到completeEvent事件后,如果还有客户正在订阅该产品是否应该马上重新向RFA重新订阅该产品.
否则客户不能收到该产品.

11.2014年7月10日.Eric要求开发一个小程序用来记录接收到的订单,方便和原始数据进行对比.测试新机房的数据稳定性.准备创建一个新的分支完成这个开发.

12.服务器监控程序开发.服务器网路是否断开.生成的数据是否正常.服务器退出邮件发送.